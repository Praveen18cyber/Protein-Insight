services:
  - type: web
    name: protein-insight
    env: node
    buildCommand: npm install && npm run build
    startCommand: node --max-old-space-size=150 dist/index.cjs
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
