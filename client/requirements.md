## Packages
ngl | 3D protein visualization library
recharts | Data visualization for interaction statistics
@tanstack/react-table | Headless table library for sorting and filtering interactions
clsx | Utility for conditional classes
tailwind-merge | Utility for merging tailwind classes

## Notes
NGL Viewer requires careful handling of the window resize event.
File uploads are handled as text content for PDB files (client reads file, sends string).
API returns analysis results as JSON, but also supports CSV download endpoints.
